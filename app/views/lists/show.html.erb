  <div class="banner" style="background-image: linear-gradient(rgba(0,0,0,0.3), rgba(0,0,0,0.3)), url(<%= cl_image_path @list.photo.key, height: 300, width: 400, crop: :fill %>)">
    <h1 class = "text-center mb-10">Catégorie <%= @list.name %> </h1>
  </div>
<div class="container">
    <div class="row">
      <div class="cards">
        <% @list.bookmarks.each do |bookmark| %>
          <div class="card-trip">
            <img src= <%=bookmark.movie.poster_url  %> />
            <div class="card-trip-infos">
              <div>
                <h2><%= bookmark.movie.title %></h2>
                <p><%= bookmark.comment %></p>
              </div>
              <h2 class="card-trip-pricing"><%= bookmark.movie.rating %></h2>
            </div>
          </div>
        <% end %>
      </div>
    </div>
</div>

<hr>
<%= link_to 'Add a bookmark', new_list_bookmark_path(@list) %>
<%= link_to "Delete",
            list_path(@list),
            method: :delete,
            data: { confirm: "Are you sure?" } %>
<%= link_to 'Back to all', lists_path %>
