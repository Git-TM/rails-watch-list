
<div class="banner" style="background-image: linear-gradient(rgba(0,0,0,0.4),rgba(0,0,0,0.4)), url(https://raw.githubusercontent.com/lewagon/fullstack-images/master/uikit/background.png);">
  <div class="container">
    <h1>All my films in one database!</h1>
    <p>Remember easily the films you saw, and all the comments you made !</p>
  </div>
</div>
<h1 class ="text-center">Toutes les listes</h1>
  <div class="cards">
    <% @lists.each do |list|%>
      <% if list.photo.attached? %>
        <div class="card-category" style="background-image: linear-gradient(rgba(0,0,0,0.3), rgba(0,0,0,0.3)), url(<%= cl_image_path list.photo.key, height: 300, width: 400, crop: :fill %>)">
      <%else %>
        <div class="card-category">
      <% end %>
        <%= link_to list.name, list_path(list) %>
      </div>
    <% end %>
  </div>



<%= link_to 'Add a list', new_list_path %>
